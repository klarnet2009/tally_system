#pragma once
// Скопируй этот файл в secrets.h (рядом, в tally_hub/src/) и заполни.
// secrets.h в .gitignore и в репозиторий не попадает.

#define WIFI_SSID   "YOUR_WIFI_SSID"
#define WIFI_PASS   "YOUR_WIFI_PASS"

// IP ATEM строкой, либо пусто ""
#define ATEM_IP_STR "192.168.1.100"
